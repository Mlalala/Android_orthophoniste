<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.insa_cvl.orthophonie">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:label="@string/title_arti" android:name=".ArticulationActivity">
        </activity>

        <activity android:label="@string/title_phono" android:name=".PhonologyActivity">
        </activity>

        <activity android:label="@string/title_phono_menu" android:name=".PhonologyMenuActivity">
        </activity>

        <activity android:label="@string/title_visu" android:name=".VisualActivity">
        </activity>
    </application>

</manifest>